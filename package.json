{
  "name": "gonnez",
  "version": "1.0.1",
  "main": "main.go",
  "private": true,
  "scripts": {
    "update": "go get github.com/JKinnez/gonnez && go mod tidy",
    "lint": "revive -config revive.toml -formatter friendly ./...",
    "test": "ginkgo -r",
    "test:deep": "ginkgo -r --fail-on-pending --cover --trace --race"
  }
}
